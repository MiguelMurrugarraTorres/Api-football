name: football_news_app
description: A new Flutter project.

publish_to: 'none'
version: 1.0.5+8

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  http: ^1.1.0
  url_launcher: ^6.2.4
  shared_preferences: ^2.2.3
  pull_to_refresh: ^2.0.0
  webview_flutter: ^4.4.2
  firebase_core: ^2.27.0
  firebase_messaging: ^14.7.10
  flutter_local_notifications: ^17.1.2
  uuid: ^4.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.3
  flutter_launcher_icons: ^0.13.1

flutter_icons:
  android: "launcher_icon"        # nombre del recurso Android
  ios: true                       # genera también para iOS (puedes poner false si no lo necesitas)
  image_path: "assets/Primerfoot_icon.png"
  image_path_ios: "assets/Primerfoot_icon.png"
  # --- Opcional (si quieres icono adaptativo en Android 8+):
  # adaptive_icon_background: "#FFFFFF"             # o "assets/icon_bg.png"
  # adaptive_icon_foreground: "assets/icon_fg.png"  # tu logo recortado

flutter:
  uses-material-design: true
  assets:
    - assets/Primerfoot_icon.png
